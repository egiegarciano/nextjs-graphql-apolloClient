query getCurrentAdmin {
  currentAdmin {
    id
    name
    email
    access_token
    role
  }
}

query adminPetList {
  pets {
    id
    name
    type
    image
  }
}

query getOnePet($id: Int!) {
  getPet(id: $id) {
    id
    name
    type
    image
  }
}
